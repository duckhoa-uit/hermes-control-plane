node_modules/
dist/
.wrangler/
# Flue generates these from the current checkout. They contain absolute
# source paths and must never be shared between machines.
.flue-vite/
.flue-vite.wrangler.jsonc
.dev.vars
*.log
.DS_Store
src/runner/sandbox-runner.ts.txt

# Vitest coverage + junit reports. Generated by `bun run test:coverage`
# and uploaded as CI artifacts by `.github/workflows/lint.yml`.
reports/
coverage/
